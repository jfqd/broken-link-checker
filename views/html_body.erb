<html>
  <head>
    <title>Broken Link Checker Report for <%= url %></title>
  </head>
  <body>
    <p>
      Crawled <%= pages_counter %> pages on <a href="<%= url %>"><%= url %></a> and found <%= array.size %> broken links in <%= time[:links].round(0) %>s
    </p>
    <% if array.size > 0 %>
      <table>
        <thead>
          <tr>
            <th>Status</th>
            <th>Broken Link</th>
            <th>Page containing the link</th>
          </tr>
        </thead>
        <tbody>
          <% array.each do |a| %>
            <tr>
              <td><%= a[:code] %></td>
              <td><%= a[:url] %></td>
              <td><%= a[:referer] %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
      <p>To ensure a good SEO ranking for your page, you should fix these broken links!</p>
    <% else %>
      <p>Congratulations! Nothing to do!</p>
    <% end %>
    <% if images.size > 0 %>
      <p>
        Crawled <%= pages_counter %> pages on <a href="<%= url %>"><%= url %></a> and found <%= images.size %> broken images in <%= time[:images].round(0) %>s
      </p>
      <table>
        <thead>
          <tr>
            <th>Status</th>
            <th>Broken Image</th>
            <th>Page containing the Image</th>
          </tr>
        </thead>
        <tbody>
          <% images.each do |i| %>
            <tr>
              <td><%= i[:code] %></td>
              <td><%= i[:url] %></td>
              <td><%= i[:referer] %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
      <p>To ensure a good SEO ranking for your page, you should fix these broken images!</p>
    <% else %>
      <p>Congratulations! Nothing to do!</p>
    <% end %>
  </body>
</html>
